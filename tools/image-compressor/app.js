document.getElementById('file-input').addEventListener('change',(e)=>{const file=e.target.files[0];if(!file)return;const reader=new FileReader();reader.onload=(event)=>{const img=new Image();img.onload=()=>{const canvas=document.getElementById('canvas');const ctx=canvas.getContext('2d');canvas.width=img.width;canvas.height=img.height;ctx.drawImage(img,0,0);canvas.toBlob((blob)=>{const url=URL.createObjectURL(blob);document.getElementById('original-size').textContent=(file.size/1024).toFixed(2)+' KB';document.getElementById('compressed-size').textContent=(blob.size/1024).toFixed(2)+' KB';document.getElementById('download-compressed').href=url;document.getElementById('download-compressed').download='compressed-'+file.name;document.getElementById('compress-result').style.display='block';},'image/jpeg',0.7);};img.src=event.target.result;};reader.readAsDataURL(file);});

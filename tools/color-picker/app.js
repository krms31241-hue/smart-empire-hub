const colorInput=document.getElementById('color-input');function updateValues(){const hex=colorInput.value;const r=parseInt(hex.slice(1,3),16);const g=parseInt(hex.slice(3,5),16);const b=parseInt(hex.slice(5,7),16);document.getElementById('hex-value').value=hex;document.getElementById('rgb-value').value=`rgb(${r}, ${g}, ${b})`;const max=Math.max(r,g,b)/255,min=Math.min(r,g,b)/255;let h,s,l=(max+min)/2;if(max===min){h=s=0;}else{const d=max-min;s=l>0.5?d/(2-max-min):d/(max+min);switch(Math.round((r/255-max)/d*60)%360){case 0:h=0;break;case 1:h=60;break;case 2:h=120;break;case 3:h=180;break;case 4:h=240;break;case 5:h=300;break;}document.getElementById('hsl-value').value=`hsl(${Math.round(h)}, ${Math.round(s*100)}%, ${Math.round(l*100)}%)`);}colorInput.addEventListener('input',updateValues);document.getElementById('copy-hex').addEventListener('click',async()=>{await navigator.clipboard.writeText(document.getElementById('hex-value').value);alert('Copied!');});updateValues();
